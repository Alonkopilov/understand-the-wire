apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: external-secrets-release
  namespace: flux-system
spec:
  interval: 10m

  path: ./k8s/infrastructure/external-secrets/release

  prune: true

  sourceRef:
    kind: GitRepository
    name: flux-system